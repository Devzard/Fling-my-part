import React, { useState, useEffect } from "react";

function DG_addpost_link({
  fonts,
  textColor,
  textSize,
  blocks,
  setBlocks,
  toggleAddMore,
}) {
  return <div>LINk</div>;
}

export default DG_addpost_link;
